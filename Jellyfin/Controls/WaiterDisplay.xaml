<UserControl
    x:Class="Jellyfin.Controls.WaiterDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Jellyfin.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Jellyfin.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance viewModels:WaiterDisplayViewModel}">

    <Grid>
        <ContentPresenter Content="{x:Bind Child}"/>

        <Grid Grid.Row="0"
              Background="{StaticResource OverlayColorBG}"
              Visibility="{Binding IsWorking, Converter={StaticResource BooleanVisibilityConverter}}">
            <ProgressRing Width="250" Height="250" IsActive="True" Visibility="Visible" />
        </Grid>
    </Grid>
</UserControl>
