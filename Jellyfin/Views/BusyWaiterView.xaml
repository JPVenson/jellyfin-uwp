<Page
    x:Class="Jellyfin.Views.BusyWaiterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Jellyfin.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Jellyfin.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModels:BusyWaiterViewModel}">

    <Grid 
                Margin="0,15,0,0"
                Background="{ThemeResource Color0}">
        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Background="{StaticResource Color10}"
              Grid.Row="0">
            <Image
                Source="/Assets/OnBoardingLogo.png"
                Stretch="Uniform"
                HorizontalAlignment="Center"
                Height="80" />
        </Grid>

        <StackPanel Grid.Row="1"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Center"
                    Margin="0,15,0,0">
            <Grid Width="630">
                <ProgressBar Maximum="{Binding RetestInterval.TotalSeconds}"
                             Value="{Binding RetestCountdown}"/>
                <TextBlock Text="{Binding RetestCountdown}"
                           Style="{StaticResource BaseTextBlockStyle}"
                           FontFamily="{StaticResource JellyfinFamilyFont}"
                           FontSize="{StaticResource FontM}"/>
            </Grid>

            <TextBlock Text="{Binding RetestMessage}"
                       Style="{StaticResource BaseTextBlockStyle}"
                       FontFamily="{StaticResource JellyfinFamilyFont}"
                       FontSize="{StaticResource FontM}"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Center"
                    Grid.Row="2">
            <Button Content="Connect now"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding InterruptRetestLoopCommand}"/>
            <Button Content="Abort"
                    Margin="15,0,0,0"
                    Style="{StaticResource SecondaryButton}"
                    Command="{Binding ExecuteAbortWaitingCommand}"/>
        </StackPanel>
    </Grid>
</Page>
